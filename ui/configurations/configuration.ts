import { configuration as development } from "@funk/ui/configurations/configuration.development"
// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `configuration.ts` with `configuration.production.ts`.
// The list of file replacements can be found in `angular.json`.

export const configuration = {
  ...development,
}
