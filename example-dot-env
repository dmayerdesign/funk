# Copy this file to create `development.env`, `staging.env`, and `production.env`, and substitute these variables
# with the appropriate values for each environment configuration.
export PROJECT_ID=
export PATH_TO_ADMIN_CREDENTIALS_JSON=/path/to/owner/service_account.json
export PATH_TO_APPLICATION_CREDENTIALS_JSON=/path/to/app_engine/service_account.json
