import { construct } from "@funk/model/commerce/sku/behaviors/get-price-after-discounts"

describe.skip("getActualPrice", () =>
{
  it("should construct", () =>
  {
    expect(construct()).toBeTruthy()
  })
})
